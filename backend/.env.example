# Copy to .env and fill in your keys. Never commit .env or your real keys.

# Option A: Gemini (default)
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Option B: Cursor Cloud API (when both set, Cursor is used instead of Gemini)
# Get API key from Cursor Dashboard → Integrations
# GitHub token must have repo create/push access; Cursor account must have access to those repos
CURSOR_API_KEY=your_cursor_api_key_here
CURSOR_GITHUB_TOKEN=your_github_token_for_cursor_agent
