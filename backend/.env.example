# Copy to .env and fill in your keys. Never commit .env or your real keys.

# Option A: Gemini (default)
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Option B: Cursor Cloud API (set CURSOR_API_KEY; users connect GitHub in the app)
# Get API key from Cursor Dashboard → Integrations
# Users provide their GitHub token in the app. Or set CURSOR_GITHUB_TOKEN for server-side repo creation.
CURSOR_API_KEY=your_cursor_api_key_here
# CURSOR_GITHUB_TOKEN=optional_server_side_token
